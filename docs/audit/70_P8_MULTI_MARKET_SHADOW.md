# P8 Multi-market shadow

`--global-shadow` loads CurrentWorldState, discovers Capital markets read-only, ranks Opportunity, writes DecisionProposals, journals prospective research (`15m` `1h` `4h` `1d`).

This run: 4 markets analyzed, 0 watch, 0 setup, 0 demo candidate, 4 blocked (no Legacy setup, not calibrated). Broker mutation = NONE.

ATTENTION_SCORE_V1 weights remain frozen.
