# Prospective capture — FLOW_EXHAUSTION_V1

Records are append-only JSONL.

`signals.jsonl` stores the frozen V1 classification at signal time.
`outcome_known` must be false at write. Outcomes are labeled later in a separate file.

Start: 2026-09-13 onward. No retroactive edits.
